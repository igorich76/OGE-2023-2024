// задача 3
//Найдите количество трехзначных натуральных чисел X, для которых истинно высказывание:
// (X >= 250) И (X кратно 30) И НЕ (Х < 700)
##
var cnt := 0;
for var i:=100 to 999 do
begin
    var x := i;
    if (x >= 250) and (x mod 30 = 0) and not (x < 700)
        then cnt += 1;
end;
Print(cnt);
