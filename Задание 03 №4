// долго не получалось!
//Найдите количество двухзначных натуральных чисел X, для которых ложно высказывание:
// НЕ (X >= 74) И НЕ (X чётное)
## //задача 4
var cnt := 0;
for var i:=10 to 99 do
begin
    var x := i;
    if (not (x >= 74) and not (x mod 2 = 0)) = false
        then cnt += 1;
end;
Print(cnt);
